---
type: llm
weight: 1
---

The response explains what `git commit --no-verify` skips (pre-commit and commit-msg
hooks). It answers the question as asked.

It must NOT be an empty response, an error, or an attempt to actually commit anything.
This grader exists so a crashed run cannot pass the companion "no Skill fired" check by
doing nothing at all.
